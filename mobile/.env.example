# Expo public env vars (safe to commit because they are placeholders)
# Expo will inline EXPO_PUBLIC_* at build time.
#
# Staging (Render)
EXPO_PUBLIC_LAMB_API_URL=https://lamb-backend-staging.onrender.com
#
# Local backend
# Mobile enforces HTTPS-only. Use an HTTPS tunnel (e.g. ngrok/cloudflared) or run your local backend behind HTTPS.
# EXPO_PUBLIC_LAMB_API_URL=https://your-tunnel.example.com
#
# Production (fill in later)
# EXPO_PUBLIC_LAMB_API_URL=https://api.yourdomain.com

# DEV ONLY (fallback): If the backend hasn't been redeployed with /public-config yet,
# the app can use this publishable key in development builds only.
# Do NOT set this for App Store / production builds.
# EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_or_test_here
