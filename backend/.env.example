# LAMB Backend Environment Variables

# Apple In-App Purchase (iOS subscriptions)
# App Store Connect → App-Specific Shared Secret
APPLE_IAP_SHARED_SECRET=

# Server Configuration
PORT=3001
NODE_ENV=development

# Firebase Admin (optional)
# Provide ONE of the following credential options:
# 1) GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/serviceAccountKey.json
# 2) FIREBASE_SERVICE_ACCOUNT_JSON=<json or base64(json)>
# 3) FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY

# GOOGLE_APPLICATION_CREDENTIALS=

# FIREBASE_SERVICE_ACCOUNT_JSON=

# FIREBASE_PROJECT_ID=
# FIREBASE_CLIENT_EMAIL=
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Set to 'true' to require Firebase auth on protected endpoints
REQUIRE_FIREBASE_AUTH=false

# Email notifications (optional)
# Used for: vault invitation emails and account change notifications.
#
# Supported providers:
#   - EMAIL_PROVIDER=sendgrid  (recommended on Render)
#   - EMAIL_PROVIDER=smtp
#   - EMAIL_PROVIDER=none
#
# Required:
#   EMAIL_FROM="LAMB <no-reply@yourdomain.com>"
EMAIL_PROVIDER=none
EMAIL_FROM=

# Optional: operator alert target (subscription processing / email failures)
ADMIN_ALERT_EMAIL=

# SendGrid
SENDGRID_API_KEY=

# SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false

# Optional branding
PUBLIC_APP_NAME=LAMB

# Remote wipe script (DANGEROUS)
# The script is dry-run by default and requires explicit confirmation to execute.
# Run: `cd backend && npm run wipe-remote`
REMOTE_WIPE_CONFIRM=
ALLOW_FIREBASE_WIPE=false
